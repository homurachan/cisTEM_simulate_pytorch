# cisTEM_simulate_pytorch

A pytorch version of cisTEM simulate. Great thanks to ChatGPT! The original version can be found at:  
https://github.com/timothygrant80/cisTEM

## Required libraries

pytorch, numpy, scipy, mrcfile

# Usage

To begin with, you need a PDB file and a RELION STAR file containing sampling points.

The sampling file can be generated by `generate_healpix_order_and_relion_star.py`. You need to place `eqps_from_fortran.py` and `healpix.py` in the same folder.

I prefer using the following command:

`python generate_healpix_order_and_relion_star.py --o c1_3deg.star --useEQPS --EQPSangleDegree 3.0 --apix 1.5`

This generates a sampling file with a 3.0-degree interval.

Then, run run_sim_from_star_parallel.py to perform the simulation.

For example:
`python run_sim_from_star_parallel.py cistem_simulate_torch_direct_slabs.py 5xnl.pdb c1_3deg.star run_sim_5xnl.mrcs --output-star run_sim_5xnl.star --device cuda --jobs 8 --gpu-id 0,1,2,3,4,5,6,7 --pixel-size 1.5 --box 320 --defocus-min-um 0.8 --defocus-max-um 2.0 --n-slices 50 --keep-tmp --inverse-contrast &`

Line 350 of run_sim_from_star_parallel.py stores the default options:

cmd.extend(["--mode", "multislice","--explicit-water", "--radiation-damage","--radiation-damage-where","all","--per-frame","--use-cache-atom","--no-center","--shake-waters"])

--mode multislice means that multislice propagation is used. You can also choose projection, which performs a simple integral projection.

--explicit-water must be used together with --shake-waters. Otherwise, severe artifacts will appear in the final images. This function simulates the movement of water molecules in the background of protein.

--radiation-damage applies a radiation-damage filter to the image. --radiation-damage-where can be set to all for both protein and water, or to protein for protein alone.

--use-cache-atom is a speed-up feature for the Python version. It precomputes the atomic potentials and places them at the corresponding coordinates.

--per-frame simulates a movie of water molecules and then combines the frames.

--no-center: if this option is removed, the PDB file will be recentered according to its center of mass. Removing this option is not recommended.

--n-slices specifies how many slices are used for multislice simulation. Normally, each slice should be less than 10 nm thick. In our example, the box size is 480 Å, and 50 slices give a slice thickness of 9.6 Å.

Defocus values will be randomly generated between --defocus-min-um and --defocus-max-um.

In the original simulator, the defocus value in the output is measured by ctffind. Here I force it to half-box-diameter + real-defocus . This has been shown in my BBR paper.

The final outputs are one star file and one mrc stack. By default, the protein is black. You can add --inverse-contrast to make it white. The output STAR file can be reconstructed using relion_reconstruct.

There are other parameters for cistem_simulate_torch_direct_slabs.py. Use --help for more information. All parameters can be added to the command line of run_sim_from_star_parallel.py, such as --poisson .
